# buns3 — project context

Lightweight S3-like object storage server. **Learning project**: the maintainer writes the code; Claude advises, reviews, and runs verification probes — don't write feature code unless explicitly asked. Claude SHOULD run curl probes / scripts / tsc to verify claims empirically ("trust the wire, not the docs" is the house method).

## Philosophy

- **Not an S3 clone.** S3 is a reference point, not a requirement. Simplicity wins. S3 ideas are kept only when independently good (bucket name rules, refuse deleting non-empty buckets, 403-without-existence-check outside key scope).
- Decisions are made deliberately and documented; "decide on purpose, write it down" over accidental behavior.
- Empiricism: every claim gets verified with a probe (curl, scratch scripts, reading node_modules types/dist as ground truth). Betas lie; types lie; the wire decides.

## Stack (deliberately bleeding-edge; pin exact versions)

- **Bun** runtime. `bun run dev` = `bun --watch ./src`. Bun does NOT type-check: run `bun x tsc --noEmit` explicitly (worth doing before believing any refactor).
- **Bun workspace** (`packages/*`, **hoisted linker — pinned in `bunfig.toml [install]`**) with a `dev` catalog in root `package.json` (`workspaces.catalogs.dev`): `@types/bun` and `typescript` are pinned THERE, referenced as `"catalog:dev"` everywhere — one edit moves every workspace. **Why hoisted (RE-decided 2026-09-03, isolated was the first choice):** the isolated linker builds `node_modules` out of Windows junctions, and **Redirection Guard makes SSH-spawned processes refuse to traverse user-created junctions** ("icke betrodd monteringspunkt" / untrusted mount point) — `bun test` and the TS server failed with "Cannot find module '@prisma/orm-sqlite/runtime'" from every Remote-SSH terminal while passing on the desktop; `Test-Path` on the junction target was True, only the link was untraversable. Hoisted = plain directories, works in every session type; running installs elevated ("trusted" junctions) was rejected as a trap. The SDK's no-Bun-globals guard is `"types": []` in its tsconfig (with `@types/bun` installed for tests via `tsconfig.test.json`) — a stray `Bun.*` in `src/` is a compile error, not a portability bug shipped to npm; the linker layout is irrelevant to it. **Never switch linkers on a live `node_modules`** — it leaves a half-migrated tree (`.old_modules-*` backup, "Cannot find name 'process'"). Fix is `rm -rf node_modules && bun install`, not config. Root `tsconfig.json` excludes `packages/` — each workspace typechecks itself (the SDK's DOM lib vs the server's Bun types).
- **Elysia 2.0 beta** (pinned). Plugins must be their `2.0.0-*` releases — npm `latest` tags still point at 1.4-era builds whose peer ranges falsely allow 2.0. Check dist-tags (`next`) before adding any plugin.
- **Prisma Next (Prisma 8 RC)** — contract-first: `src/modules/prisma/contract.prisma` → `bunx prisma contract emit` → migration plan → apply. READ migration plans before applying (no rename detection; renames render as destructive drop+add). CLI: `--json` gives full error envelopes where `-v` shows nothing.
  - `migration plan` never chains from the newest migration on disk — origin is `--from`, else the `db` ref (`migrations/app/refs/db.json`, committed), else empty (refused). Apply with `db migrate --advance-ref db` or the ref lags and the next plan needs `--from` again — **locally only**: the flag writes back into the source tree, so the container runs bare `db migrate` (see deployment).
  - **No `@default(now())` on DateTime** — it renders `datetime('now')`, whose naive string the `sqlite/datetime@1` codec parses as *local* time on read (tz skew; upstream bug candidate). All timestamps are stamped from JS (`new Date()` round-trips ISO-Z on every lane). DB defaults are allowed only when they can solely fail closed (`publicRead 0`) — and the write path still stamps explicitly; the default is the migration backstop.
  - **ORM gotchas (orm-sqlite rc.8, all probed)**: no terminal `.count()` on collections — use `.aggregate((a) => ({ n: a.count() }))`; bare `.count` resolves to the include-reducer and throws ORM.INCLUDE_INVALID. Collection-form `.include("rel")` type-collapses rows to `{[x:string]: unknown}[]` (runtime probably fine, types broken) — reducer form (`(o) => o.count()`) types correctly; query the relation's own collection directly instead. Chained `.where()` AND-composes and keeps the collection generic (safe to `let`-reassign for conditional filters). Bulk delete = `.select(...).deleteAll()` — RETURNING-style, hands back the rows actually deleted (use as ground truth over any pre-select). `.in([...])` with 1000+ bound params is fine — this build carries SQLite's modern 32766 variable limit (probed), not the historical 999.
- **arktype** for validation (Standard Schema). `string.integer` VALIDATES a string (stays string); `string.integer.parse` MORPHS to number — `.to("number...")` on the unparsed form is an unsatisfiable string∩number intersection that throws at module load (= server down, schemas build at import). **`@elysia/openapi` is locally patched** (`patches/`) — its dist/gen had broken relative typebox imports; patch reapplies on install via `bun patch`.

## Architecture

- `src/config.ts` — **every env read goes through here; `process.env` is not read anywhere else in `src/`.** One arktype schema (`Config`, exported for tests), parsed ONCE at import into a frozen `config` (named for the concept, not the source — other sources than env may feed it later); invalid config lists every problem via `summary` and exits — bad config means no server, never silently wrong behaviour. **Empty strings are OMITTED before parsing** (a key present as `undefined` is NOT missing to arktype, the default would not apply — probed), which structurally closes the compose `${VAR}`→`""` trap. `.onUndeclaredKey("delete")` so the frozen object does not carry the whole environment (tokens) into anything that logs it. Flags are a closed `"0" | "1"` vocabulary — `OPENAPI=true` is a boot error, not a guess. Integers are `string.integer.parse` → `.to(bounds)` → `.default("<raw string>")` (the default is the pre-parse STRING; `string.integer` without `.parse` + `.to(number)` throws at module load — the documented arktype trap). `BASE_URL` is required and narrowed to an exact origin (`new URL(v).origin === v`), because presign assembles URLs by string. Cleanup timings carry a 60000 floor: a small gate is a data-eater by config. `parseEnv(raw)` is the testable seam (29 tier-1 tests in `src/__tests__/config.test.ts`, incl. `""`→default). The test preload sets `BASE_URL` explicitly so the fixture never depends on a gitignored `.env`.
- `src/lib/` — framework-free (no Elysia imports; enforced by comment in error.ts): error codes, `Buns3Error`/`Buns3ValidationError`, `unwrap`, key/encoding helpers (`strictEncode`, `uriEncodedKey`, `keyToFilename`).
- `src/modules/storage/` — fileStorage + bucketStorage. **Results-as-values, never throws.** Flat blob layout: `data/<bucket>/<uuid>`, metadata in SQLite; keys are opaque strings that never touch the filesystem. Write-order invariants: blob durable before pointer commit; pointer dropped before blob unlink; temp files in `data/.tmp` then rename (same-fs atomic). Failed unlinks are logged orphans, not client errors.
- `src/modules/api-keys/` — keyStorage. Tokens `buns3_` + 32 bytes base64url; SHA-256 hex digest stored (never the token; hash never leaves the server); `toApiKey` mapping allowlists fields and converts 0/1→boolean (SQLite target has NO boolean codec — capability columns are Int). verify = format regex → hash → single find-and-touch UPDATE (also stamps lastUsedAt). `getAll` (unfiltered, mapped — never leaks hashes), `delete(id)` (null-from-ORM → API_KEY_NOT_FOUND 404). **Keys are immutable** — no update op, ever; rotation = mint new + delete old (decided).
- `src/modules/http/` — Elysia shell. `server.ts` is a composition root only (error handler, openapi, `.use()` per route file, listen — plus the one deliberate exception: the inline `GET /` healthcheck, a one-liner that would be ceremony in its own route file); routes live in `routes/*.routes.ts`, one file per resource, each a named plugin (`routes:*`; macros in `middleware.ts` are `middleware:*`) that `.use()`s the macros it needs (dedupe by name makes this free). Handlers `unwrap()` storage results into throws; central error rendering. Auth guards are declared ONCE per scope: group-level where a group exists (`/keys`), per-route otherwise — no redundant doubling.
- Bucket rows never hit the wire raw: `storage/mapping.ts` (`toBucket`/`toBucketWithCount`) allowlists fields and booleanizes `publicRead` — same lesson as `toApiKey`.
- `src/modules/auth/` — framework-free auth logic, results-as-values: `resolveCredentials(authorization, query)` → `Credentials` (bearer | presign | anonymous; both-presented or malformed presign params → INVALID_API_KEY), `authorize({state, capability, bucket, key, method})` dispatches on `AuthState` (key | presign | anonymous) to per-kind guards. The Elysia macro is a thin adapter over these — do NOT reinline logic into it.
- `src/lib/presign.ts` — pure HMAC presign core: `canonicalString` (`buns3-presign-v1\n` + method\bucket\key\expires, newline-delimited — collision-safe ONLY because bucket/key validators exclude control chars), `sign`/`verify` (HMAC-SHA256 keyed by the stored tokenHash hex string; constant-time compare behind a strict 64-hex regex gate — `Buffer.from(hex)` truncates silently and `timingSafeEqual` throws on length mismatch), `deriveKeyId` (HMAC over a keyid domain string — public fingerprint, NEVER a secret), `buildPresignedUrl` (sign the RAW key, encode via `uriEncodedKey` — the same strictEncode-per-segment the PUT Location header uses, so every object has ONE URL spelling; plain string assembly, never `new URL()` — it normalizes dot-segments and discards any base path; base must be an ORIGIN). Clock and hash injected; no throws. Shared by CLI + API endpoint — never duplicate the URL assembly.
- `src/modules/validation/` — arktype schemas. `CreateApiKey` is a discriminated union: global admin (isAdmin true, bucketName null, canRead/canWrite false) | bucket data key (isAdmin false, bucketName required, ≥1 of read/write via narrow). Bucket names: `^[a-z][a-z0-9-]*$` ≤20 — can't start with `_`, which structurally reserves the whole `_`-prefix namespace for non-bucket routes: `_admin`, `_self`, `_openapi` all live there, and ANY new top-level (non-data) route MUST take a `_` prefix or it can be shadowed by / collide with a valid bucket name (e.g. `openapi` is a valid bucket — see b355f07). **One deliberate exception: `GET /favicon.ico` → 204.** Browsers request that exact path, and it otherwise matches `/:bucket` and 422s on the bucket-name pattern (noise on every page view of the tester pages). Safe because `favicon.ico` contains a dot, so it can NEVER be a valid bucket name and cannot shadow one — the exception is structural, not convenience, and does not generalise to other un-prefixed root routes. Keys: any non-control chars ≤1024, not slash-only, no `.`/`..` path segments (unaddressable via URL path).

## Auth model

- Independent capabilities (no hierarchy): canRead, canWrite, isAdmin. **Admin = control plane only** (cannot read/write objects; mints itself a data key when needed). Data keys are always bucket-scoped; admin keys always global. write does NOT imply read.
- Elysia macro `auth: (capability?: "read"|"write"|"admin"|true)` — `true` = any valid key (whoami). derive resolves credentials → `AuthState` (bucket-blind: bearer verifies now, presign defers); beforeHandle calls `authorize` (bucket-aware: presign sig verification, capability + scope). This split is load-bearing: derives run before beforeHandles, guaranteeing bucketKey validation (422) precedes auth judgments — presign sigs are computed over bucket+key, so verification MUST stay in beforeHandle. Scope mismatch → 403 API_KEY_SCOPE_MISMATCH without consulting bucket existence (S3-style, no existence oracle). whoami reads `authState` (narrows kind === "key"). Key-auth codes are the `API_KEY_*` family (renamed fc6289d — unprefixed `KEY_*` collided with object-land's KEY_NOT_FOUND).
- **Presigned URLs** (query params `keyId`/`expires`/`sig`): offline-signable — sig = HMAC-SHA256(sha256(token), method|bucket|key|expires), keyId derived from the hash client-side, zero schema changes, revocation = delete the key. Any presign param present → presign path (all three must validate; partial/malformed → 401 — never falls back to anonymous). Both header AND params → 401 (ambiguity rejected). Expired → 401 PRESIGNED_EXPIRED (own code group); every other failure collapses to 401 INVALID_API_KEY (no oracle). Verified signer becomes an ordinary principal through the same authorizeKey guards: wrong-bucket signer → 403 API_KEY_SCOPE_MISMATCH, admin signer → 403 API_KEY_NOT_CAPABLE. Data-plane only (no bucket/key on route → 401). Expiry inclusive (`now <= expires`, unix seconds), **TTL capped at 7 days** (604800, S3 parity — RE-decided from "no cap": presigned URLs are unrevocable-except-by-key-deletion grants, the cap bounds leaked-link exposure; longer needs = mint a scoped key), Content-Type unsigned (decided). **ttl is a DURATION; expires is an ABSOLUTE timestamp whose bound (999999999999) is a format check** — one number must never serve both (this drifted 3× in one evening; now pinned by the mint/verify bound-invariant tests). keyId lookup = O(n) scan in verifyPresigned (indexed derived column is the upgrade path). DB-leak trade documented: stored hashes become forge-capable for presign (not for bearer).
- **`POST /_self/presign`** (977bae6): a key presigns FOR ITSELF — body `{method, bucket, key, ttl}` → `{url}`. Capability check = `authorize` with `methodCapabilityMap[method]` (GET/HEAD→read, PUT/DELETE→write; `satisfies`-total over PRESIGN_METHODS) — a key can only presign what it could do itself, so admin keys structurally can't presign (403 API_KEY_NOT_CAPABLE) and wrong-bucket → 403 API_KEY_SCOPE_MISMATCH. Signing happens INSIDE apiKeyStorage.presign (hash never leaves the module; row-gone → INVALID_API_KEY — the caller's own credential died). Presigned creds can't call it (no bucket/key on route → 401): URLs can't mint URLs. Admin tester's Presign section is wired to it (own data-key field).
- 401 (INVALID_API_KEY, with WWW-Authenticate: Bearer) = missing/malformed/unknown credentials; 403 = authenticated but insufficient. Bearer scheme required, case-insensitive prefix.
- **Public-read buckets** (`publicRead`, deny by default, toggled via `PATCH /_admin/buckets/:name`): the anonymous fallback fires ONLY when no Authorization header is present at all, and only for capability "read". Presented-but-invalid credentials → 401 even on a public bucket (S3 semantics). Anonymous on private and nonexistent buckets both → uniform 401 (no existence oracle — the fallback's bucket lookup must not unwrap). **A presented key is always judged as that key**: admin or wrong-bucket data keys get 403 on a public bucket while anonymous gets 200 — deliberate divergence from S3 (credentials opt you into evaluation; drop the header to read publicly).
- Bootstrap: `bun scripts/create-key.ts --name X [--bucket B] [--read] [--write] [--admin]` — prints token ONCE. `/_admin/keys` POST is admin-gated; key zero comes from the script.

## Error handling

- Storage returns `{success, code}` results; HTTP throws `Buns3Error(code)` (via `unwrap`) or `Buns3ValidationError(arkErrors)`.
- `http/error.ts`: `ERROR_STATUS` table `satisfies Record<Buns3AnyErrorCode, number>` (compiler-enforced totality — adding a code without a status breaks the build). Per-class `.error()` registrations render problem+json (RFC 9457) with `code` as extension member; validation errors carry arktype `summary` in `detail`. 500s: generic detail + correlation ref, full error logged server-side. Fallback registration must never throw.
- SQL error translation via structural guards in storage/errors.ts: `isUniqueViolation` (sqlState 23505), `isFkViolation` (23503). SQLite never names FK constraints (`constraint: undefined`) — one Restrict-FK per operation context max, disambiguate by context not constraint.

## Elysia 2.0 gotchas (all learned the hard way)

- Streaming routes need `parse: 'none'` — the parser consumes recognized content-types even without a body schema. Raw stream: destructure `{ request }`, use `request.body`. Never declare body schemas on streaming routes.
- **Params arrive pre-decoded** — do NOT decodeURIComponent again (double-decode aliases keys with literal %).
- `ctx.params` is **undefined** on param-less routes (types claim otherwise) — use `params?.`.
- Wildcard `/:bucket/*` also matches single-segment paths (`/foo` → empty key → NotFound thrown).
- Macros: options-before-handler arg order works; `beforeHandle` isn't suggested by autocomplete but type-checks; macro's own derive output isn't typed in its beforeHandle ctx (annotate the destructured param) — but IS typed correctly in route handler ctx. Macro options at GROUP level (`.group("/keys", { auth: "admin" }, ...)`) guard all children — verified on the wire; don't duplicate per-route.
- Bare `Context` in a hand-typed hook annotation re-asserts `params` as REQUIRED (the params lie, type-approved) — intersect via `Omit<Context, "params">` and re-declare `params?:` optional.
- Repeated query params are last-wins strings (`?sig=a&sig=b` → `"b"`), never arrays; empty value → `""`, absent → undefined. `Record<string, string | undefined>` is wire-true (probed).
- **Macro derive order = the route's OPTIONS-OBJECT KEY ORDER, not plugin registration order** (probed: `{a, b}` runs A's derive first, `{b, a}` runs B's). Anything that must precede auth (bucket/key validation → 422) therefore lives in the macro's `transform` hook, which runs before every derive regardless of ordering; the derive only reads back the validated params for typing. `resolve` no longer exists on macros in 2.0 (merged into derive) — transform is the only earlier phase.
- A macro derive returning a UNION of shapes (`{apiKey: null} | {apiKey: ApiKey}`) poisons the context merge — downstream `params` collapse to `never`/`undefined` in unrelated routes. Return one shape typed `ApiKey | null`.
- Returning BunFile gets free HTTP range support (206). Don't set Content-Length on GET (framework computes, range-aware); DO set it manually on HEAD from object.size, and return a raw `new Response(null, {headers})` (returning `{}` serializes to 2-byte body).
- Elysia auto-strips bodies on HEAD responses — no isHead special-casing needed in error handlers.
- Schema-slot validation loses Standard Schema (arktype) issue messages (beta bug) — admin routes validate MANUALLY in handlers (`CreateApiKey(body)` → throw Buns3ValidationError). Revisit when fixed upstream.
- **Plugin lifecycle hooks are LOCAL unless `.as("global")`** — a `.afterResponse()` inside `new Elysia({ name })` fires for none of the parent's routes; the per-hook `{ as }` option is a no-op in 2.0 (probed). `onRequest`/`onAfterResponse` do not exist; the earliest hook is `transform`, which is also the only one that runs before a macro's own transform can throw.
- openapi plugin: RegExp excludes are silently broken (`includes()` not `.test()`) — use exact Elysia route strings; data plane is excluded (wildcards inexpressible in OpenAPI). Docs at **`/_openapi`** (b355f07 — moved off `/openapi` because that IS a valid bucket name and the static route shadowed a bucket named "openapi"; the `_` prefix makes collision structurally impossible), admin plane only, Bearer scheme configured.

## Wire contract highlights

- PUT object: 201 + percent-encoded relative `Location`; overwrites also 201. Empty body allowed (zero-byte objects, S3-style). Content-Type stored verbatim from header (default application/octet-stream, no sniffing — sniffing was deliberately rejected). Max body 5GB (`serve.maxRequestBodySize`).
- GET: metadata-driven headers (stored content-type, Last-Modified UTC, RFC5987 Content-Disposition inline with encoded filename, quoted ETag = blob id). HEAD mirrors GET minus framework-computed headers. **HEAD exists ONLY on the object route** (decided): it's the one endpoint where the body is expensive; admin/self/listing are cheap JSON — use GET. Elysia does NOT auto-map HEAD→GET (probed: GET-only routes answer 404, or 422 via the wildcard for `_`-paths), so a HEAD route exists exactly where typed. The former admin bucket HEADs (one of which was the audit's unauthenticated route) are deleted, along with both storage `head()` methods (`fileStorage.head` was dead AND diverged from `get` on orphans — HEAD must agree with GET on 404, so the object HEAD route uses `get()` and pays the stat).
- DELETE object/bucket: 204; missing → 404 (deliberate, not S3's idempotent 204). Bucket delete refuses non-empty (409 BUCKET_NOT_EMPTY, count pre-check + FK backstop) and removes the empty dir best-effort.
- Keys are defined AFTER percent-decoding; clients must encode. Validation precedes auth (422 before 401/403 — S3 does the reverse; documented trade for lifecycle-ordering robustness). As of 2b44edb this holds on the ADMIN plane too (`useBucket` macro; param renamed `:name`→`:bucket` — wire-invisible): malformed bucket names 422 before auth everywhere.
- **Batch delete `DELETE /:bucket`** with JSON body `{keys: [1..1000 Keys]}` (`auth: "write"`): NO body / empty keys → 422 — absence of keys NEVER means "all keys". Duplicates deduped silently. Per-key results in request order (`{success,key}` | `{success:false,key,code:"KEY_NOT_FOUND"}`) + `{deleted,missing}` summary — missing keys are reported, never batch-failing (race-proof teardown, non-idempotency stays honest). Delete-by-ID from a pre-select ("delete the versions I saw" — concurrent overwrites survive; deliberate), results built from `.deleteAll()`'s RETURNING rows, then best-effort unlinks of exactly those ids (orphans logged, never client errors). Single statement = atomic pointer phase, no explicit tx. Presign structurally can't batch (one key per canonical string). Teardown story: list → batch delete → `DELETE /_admin/buckets/:bucket`.
- **Object listing `GET /:bucket`** (`auth: "list"` — maps to canRead for keys, structurally never anonymous: public-read grants object GET, NEVER enumeration): keyset pagination `?after=<key>&limit=1..1000` (default 100; fetch limit+1, `nextAfter` = last returned key ONLY when the extra row exists — else null), `?prefix=` via code-point range (`key >= prefix AND key < prefixUpperBound(prefix)` in `lib/key.ts`) — NEVER `.like` (SQLite LIKE is ASCII-case-insensitive and `%`/`_` are metachars; keys like `100%.txt` are fixtures). Response `{bucket, filters (effective), count, nextAfter, objects}` via `toObjectSummary` (etag = blob id). Single-segment GETs (`/foo`) now hit this route → 401, not the wildcard's 404 (deliberate wire change). Presigned URLs can't list (no object key on the route). openapi excludes `"/:bucket"` AND `"/:bucket/*"`.

## Dev setup (new machine)

1. `bun install` (applies `patches/`), copy/create `.env`: `PORT=8000`, `SQLITE_PATH=./data/db.sqlite`, `BASE_URL=http://localhost:8000`.
2. `data/` is gitignored: DB + blobs do NOT travel. Fresh machine = fresh DB: run the Prisma migration apply flow, then `bun scripts/create-key.ts --name admin --admin` for key zero, create buckets via `PUT /_admin/buckets/:name`.
3. Old tokens are hashes in the old DB — they don't transfer; mint new ones.
4. Acceptance rituals: `bun test` from the root (581 tests ~3s across the workspace — 318 server incl. tier-2 real-DB/fs via the preload fixture and route-level tests through `createServer().handle()`, 263 SDK all-fakes; run reflexively), `bun x tsc --noEmit` PLUS `-p tsconfig.integration.json` (the SDK↔server test imports SDK sources, which need the DOM lib; the root config stays narrow so a browser global in server code is still a compile error — verified both ways) and `-p tsconfig.test.json` inside `packages/sdk`, the curl matrix (47 cases) after any change to auth/routes, and **`bun run build` in `packages/sdk` after any SDK change** — it is the only check that catches an import resolving through the hoisted workspace but not for a consumer.

## Current state & queue (2026-08-31)

DONE: object storage (streaming, atomic, metadata), bucket lifecycle, API-key auth end-to-end, Elysia 2.0 port (3c2119a), OpenAPI admin docs + bucket object counts (4ef9f81), createdAt tz fix — JS-stamped timestamps, DB defaults dropped (23f5c09), public-read bucket flag end-to-end incl. anonymous fallback + PATCH endpoint (62c24ab; full 15-case curl matrix green), upload tester page — now versioned at `tools/upload-tester.html` (React UMD + Babel standalone PINNED to @7: v8's react preset emits automatic-runtime imports that break classic script tags; upload with explicit `Content-Type: text/html` or the no-sniffing rule serves it as a download), auth extraction refactor — macro logic → `modules/auth/` behind a 23-case status+code matrix, byte-identical before/after (e658d1b), **presigned URLs end-to-end** (a5282c3; 18-case lib hammer + 16-case wire matrix green, old matrix unchanged — phase 4 core done), presign CLI `scripts/presign.ts` (2476ecb; offline — zero DB imports, that's load-bearing: `hashToken` lives in `lib/presign` so minting never drags in prisma; sign the RAW key, encodeURIComponent per segment for the URL — decoding there is the double-decode alias bug mint-side; ttl 0 = legal "this second only" per inclusive expiry), **tier-1 test suite** (a8c8d08; 109 tests/~250ms, Bun Test, co-located `__tests__/` dirs per module: `lib/presign` hammer incl. sha256 anchor vector for a future browser signer, all 4 validation schemas, `resolveCredentials`), admin tester page + light/dark toggle on both testers (c4849d0; shared `buns3.theme` localStorage key, pre-paint theme script), **routes/ split** (dbe0c64; behavior-frozen, matrix byte-identical, openapi exclusion intact), **admin key list/delete** (fc6289d + 4ec145e; `GET/DELETE /_admin/keys[/:id]`, uuid → 422, unknown → 404, delete → immediate 401 for that bearer, KEY_* → API_KEY_* code rename — matrix delta was exactly the 4 rename lines, baseline regenerated).

DONE (cont.): **`/_self` credential plane** (5ebf31f) — the three-planes rule: `/:bucket/*` data plane (capability auth), `/_admin` admin plane (admin key, NO exceptions), `/_self` credential plane (`auth: true`, operates on the presented key). `GET /_self` = whoami (moved from /_admin/whoami — wire break, testers updated), `DELETE /_self` = self-revocation (204 → immediate 401 for that bearer). Future `POST /_self/presign` = presign endpoint's decided home. Underscore prefix is load-bearing for ANY new top-level route: bucket names can't start with `_`. Strays under retired `_`-paths → 422 as invalid bucket names (no lookup, no oracle; probed real S3: it answers the same case 404 NoSuchBucket + existence disclosure — our divergence is deliberate). Testers updated: whoami → /_self, admin tester has keys table + revoke. **`POST /_self/presign`** (977bae6 + tester wiring ffc3587) — see Auth model; suite now 116 tests incl. buildPresignedUrl cases + mint/verify bound invariant. **Object listing** (ff37563; wire contract section has the full design — 127-test suite incl. prefixUpperBound hammer + keyToFilename, matrix +7 listing cases green on first run; DECIDED: no `total` field — keyset never knows it, admin plane owns counts; NO delimiter/folder emulation — deferred at the S3-clone complexity cliff). **Batch delete** (248ead0; wire contract has the design) + admin-plane 422 alignment via useBucket (2b44edb). **README** (76db46d — quick start, three planes, API reference, nine-entry decisions ledger; Claude-written on request, house voice). Live-fire bonus: the original admin key was accidentally revoked mid-session — recovery via `create-key.ts` worked exactly as designed (instant uniform 401, re-bootstrap, no special cases).

DONE (cont. 2): **Tier-2 test suite** (a2458c8) — 174 tests / ~1.1s across 11 files. Fixture: `DATA_PATH` env seam in storage/constants (mirrors SQLITE_PATH), `bunfig.toml` `[test]` preload sets env into a per-run tmpdir BEFORE any project import (order is load-bearing — schemas/constants read env at import), template-copy SQLite keyed by the migration ref hash (migrate once per schema change via spawnSync `bun x prisma` — cold build ~2s, then ~ms file copy; the old "~50s cold" claim was WRONG and cost an evening: the build was never slow, an UNPINNED `bun x prisma` re-resolving `latest` against the npm registry was the only network step — `prisma` is now a PINNED root devDep (8.0.0-rc.12, the CLI that built the committed migrations; note dist-tag `next` = rc.10 is BEHIND `latest` = rc.12, betas lie) so bunx resolves `node_modules/.bin` with zero registry contact. Template cache lives at repo-root `.cache/buns3-test/` (gitignored), NOT `node_modules/.cache` — `rm -rf node_modules` is the documented linker fix and must not evict it; the build announces itself on stderr so a slow one never reads as a hang), stale-run cleanup at NEXT start (Windows locks + failure corpses stay inspectable). `test/helpers.ts`: `resetStorage()` in explicit beforeEach; seeds go through real storage APIs. Now assertions, not prose: write-order invariants (one blob + empty .tmp post-PUT, overwrite swaps blob identity), FK cascade kills scoped keys, no-oracle = literal equality of the two 401s, anonymous public-read branch, presign method-binding. ORM gotcha found: unfiltered `deleteAll` is a TYPE error (`this: never`) but executes at runtime — the WHERE-less-delete rail lives in the types; pay the always-true-predicate tax (`.where((o) => o.id.isNotNull())`) with a comment.

NEXT (in order):
1. ~~**TypeScript SDK**~~ **DONE 2026-09-04** — `packages/sdk`, publishes as **`@buns3/sdk`** (npm org `buns3` claimed 2026-09-01; bare `buns3` unregistrable — `bun-s3` placeholder squat + npm's strip-punctuation similar-name rule; scopes exempt). 263 tests (~40ms, all fakes), 8.68 kB build, ZERO runtime deps. Verified publishable by packing the tarball and installing it into a clean project: ESM types, ESM runtime and CJS require all work; 7 files ship (dist, README, LICENSE, package.json).
   - **Shape**: `src/` machinery (http, result, types/, index) + `src/planes/` (objects, self, admin, presigned) + `src/lib/` (encoding, error, params, presign); tests co-located in per-module `__tests__/`. Two clients over a shared base: `Buns3Client` (objects + `bucket(name)` binder + offline `presign`) and `Buns3AdminClient` (admin; token REQUIRED at compile time), both carrying `self` + `presigned`. **Split because no key works on both planes** — an admin key 403s on every object call, a data key 403s on every `/_admin` call, so a combined client is always half-dead. That is NOT client-side capability gating (the SDK still never rejects a request); it just does not offer what provably cannot work.
   - **Offline signer is the headline feature, and it is byte-identical to the server**: a `crypto.subtle` port cross-checked against `Bun.CryptoHasher` on hashToken/deriveKeyId/sign/buildPresignedUrl, against the `sha256("hello")` anchor vector, and end to end (offline-minted URL → 200; expired → 401 PRESIGNED_EXPIRED; tampered sig → 401 INVALID_API_KEY). **The HMAC key is the 64-char hex STRING UTF-8-encoded, not the 32 bytes it decodes to** — the one detail that fails silently. Frozen vectors live in `lib/__tests__/presign.test.ts` because the SDK cannot import the server. No `Uint8Array.toHex` feature-detect: it is Stage 3 and a second path would be untested on whichever runtime CI uses; `padStart(2)` per byte is load-bearing (its removal fails 10 tests).
   - **Lessons pinned by tests**: `route()` must NOT normalize (a `//`-collapse or trailing-slash strip makes `a//b`, `trail/`, `/lead` DIFFERENT objects); values go in RAW and each layer encodes ONCE (`route()` for paths, `URLSearchParams` for queries — pre-encoding either gives `%20`→`%2520`); `list` sends only *defined* filters (`limit: 0` must reach the server and earn its 422, not silently fall back to 100); `head` unquotes the etag so it equals a listing's; `put` always states a Content-Type (explicit > `Blob.type` > octet-stream — note `blob.stream()` LOSES the type) and sets `duplex: "half"` for streams (undici throws without it); a `ReadableStream` body is never retried (consumed by attempt 1); `500` is never retried; `Retry-After` is honored exactly, un-jittered.
   - **THREE stray `arktype` auto-imports landed this session and nothing caught them.** tsc and `bun test` both resolve any root package through the hoisted workspace, so an unused import typechecks AND passes tests while landing in `dist/` — one in `http.ts` shipped 155 kB for 8 kB of SDK. Only `bun run build` noticed, and only once `index.ts` finally exported something. Now enforced by `__tests__/dependencies.test.ts` (two passes: Bun's transpiler for value imports, plus a line-anchored regex for type-only imports, which the transpiler ERASES; mutation-checked both directions). **Run `bun run build` before believing SDK work is finished.**
   - ~~**SDK↔server integration tests**~~ **DONE 2026-09-04** — `src/modules/http/__tests__/sdk-integration.test.ts`: the real SDK against the real server with NO socket, by injecting a fetch that calls `createServer().handle()`. 26 tests — object round-trips (incl. keys with `%`, spaces, empty segments and non-ASCII), etag agreement between `head` and `list`, filters echoed, `limit: 0` earning its 422, batch-delete per-key results, the error-code families (401/403/404/422) the SDK narrows on, whoami, the server-validated presign refusing what a key could not do itself, and the **offline signer accepted by the server** (plus offline and server-minted signatures proven identical for the same inputs, compared on the SIGNATURE not the URL — the host is deliberately unsigned). Carries the server-side half of the error-code drift guard: the SDK's `ERROR_CODES` must equal the flattened `Buns3AnyErrorCode` (mutation-checked — adding a server code fails it). **Typechecked by `tsconfig.integration.json`**, not the root config: it imports SDK sources that need the DOM lib, while the root stays narrow so a stray browser global in server code is still an error (both directions verified). **KNOWN LIMITATION**: `new Request(url)` NORMALISES the URL (a raw space becomes `%20`, `ü` becomes `%C3%BC` — probed), so if the SDK stopped encoding paths this harness would silently repair it; client-side encoding is pinned instead by the SDK's frozen anchor table. Presign tests deliberately use keys with a space and a percent, because `a.txt` alone cannot tell signing the RAW key from signing the encoded one (mutation-checked).
   - **STILL OPEN**: (a) **dogfooding** — the tester pages should consume the SDK. (b) not published to npm yet.
2. ~~**Production deployment**~~ **DONE 2026-09-04** — multi-stage `Dockerfile` + `docker-compose.yml`, verified end to end on podman/Fedora (rootless) and **LIVE IN PRODUCTION** (Dokploy + Traefik + Cloudflare behind a personal domain, not recorded here). **Two targets from one Dockerfile because the migration CLI is enormous**: a full install is 964 MB (the `prisma` package drags in `@distilled.cloud` 299 MB, `alchemy` 185 MB, `@cloudflare` 89 MB, `effect`, `vite` — Prisma's cloud tooling, none of which a local SQLite deployment touches) versus 131 MB for `--production`. So `migrate` carries the CLI and exits; `runtime` serves. `@prisma/cli-engine` is NOT a lighter path (361 KB but no `bin` — it is the framework, not the command). Migrations cannot run at build time (the DB lives on the volume, which does not exist then), so they run as a one-shot before the server starts, fail-closed, proven idempotent. Both containers run as **`USER bun`**; `/data` is `mkdir`+`chown`ed in the base stage, which is what makes a NEWLY created named volume inherit `bun` ownership (an existing root-owned volume will not — testing needs a fresh one). `.env` holds `BASE_URL`/`PORT` locally; ports are published only via a gitignored `docker-compose.override.yml`, because Dokploy fronts this with Traefik and publishing would bypass TLS entirely (Bearer tokens in cleartext — and presigned URLs would validate there too, since the host is not signed). Verified: 5 migrations on a fresh volume → the committed ref hash, key zero via `podman exec ... create-key.ts`, PUT/GET round-trip with a `bun`-owned blob and an EMPTY `.tmp`, data surviving `down`/`up`, and a 0.16s graceful stop.
   - **Gotchas, each of which cost real time (all probed):**
     - **Compose NAMESPACES named volumes with the project name** — `volumes: buns3-data` becomes `buns3_buns3-data`. Removing the bare name deletes a different volume and the next run misleadingly reports "Already up to date" instead of applying migrations.
     - **`--advance-ref db` is DEV-ONLY.** It writes `migrations/app/refs/db.json` back into the source tree → `EACCES` for a non-root container, and pointless there anyway (the write lands in an ephemeral layer). The database's own marker table decides what is pending. Local authoring still needs the flag; the container must not use it.
     - **`${VAR}` with nothing set resolves to an EMPTY STRING and overrides the image's `ENV` defaults** — and empty is not nullish, so `??` does not save you: `PORT=""` makes Bun bind **3000**, `DATA_PATH=""` writes blobs to `/app` instead of the volume (both measured). Either omit the variable from compose or always use `${VAR:-default}`. Same trap in the Dockerfile: `ENV PORT=${PORT}` with no `ARG` sets it empty.
     - **`ENV KEY \` + continuation mixes the legacy and modern forms** — `ENV BASE_URL \` + `PORT=8000 ...` parses as legacy `key value`, so `BASE_URL` swallows the rest of the line as its value and the other three are never set. Also: a trailing space after `\` breaks continuation.
     - **`tsconfig.json` MUST be in the image.** Bun resolves the `$/*` path alias from it at runtime and 16 source files use it — without the file the server cannot start (probed: `Cannot find module '$/lib/...'`).
     - **`scripts/` must be copied** or there is no way to mint key zero, and a fresh deployment has no admin key, no bucket, and no way in.
     - **PID 1 ignores SIGTERM** unless the process installs a handler — the kernel applies no default action there. Without one, every stop waited out the grace period and got SIGKILLed (10s), which also abandons `.tmp` files mid-upload. `src/index.ts` now handles SIGINT/SIGTERM and calls Elysia's `stop()` (no argument = drain, do not sever active transports). `init: true` in compose is the belt-and-braces version.
     - **Podman builds OCI format, which silently DROPS `HEALTHCHECK`** with a warning. Either build `--format docker` or put the healthcheck in compose (chosen — it is per-deployment config anyway).
     - **`${BASE_URL:?err}` makes even `podman compose down` fail**, since compose interpolates the whole file for every command. A local `.env` fixes it.
     - `DO_NOT_TRACK=1` in the migrate stage: a deploy must not depend on Prisma's telemetry endpoint.
     - Bun binds `::` (all interfaces) for a bare `.listen(port)` despite reporting `hostname: "localhost"` — probed, so containers are reachable without passing a hostname.
     - Podman needs `docker.io/`-qualified image names, and a stale `~/.docker/config.json` (`credsStore: pass` from Rancher Desktop) breaks the compose provider before any build starts.
3. ~~**`Cache-Control` on object responses**~~ **DONE 2026-09-04** (`7f1e87b`, `c6320e8`) — found in production: buns3 sent no `Cache-Control`, so Cloudflare invented a 4h default and **flipping a bucket private did NOT stop the object being served** (edge answered 200 while a cache-busted request got 401); the same applied to DELETE, and a presigned URL could outlive its expiry since the signature is never re-checked. Fixed by keying the directive on **how the request was authorized**: `authState.kind` is an exact proxy for cacheability, because anonymous access only ever succeeds on a public-read bucket — no bucket lookup needed. anonymous → `public, max-age=60`; key/presign → `private, no-store` (so a PUBLIC bucket read WITH a key is still private, consistent with "a presented key is always judged as that key" and with HTTP's own rule about authenticated responses). The map is a table that `satisfies Record<AuthKind, string>` rather than a switch with a `default:`, so a fourth AuthKind is a compile error instead of a silent fall-through to `public` — verified by adding one. `Vary: Authorization` on every object response. 60s is short because **keys are MUTABLE** — max-age is exactly how long an overwrite stays invisible.
   - **Conditional requests came with it.** Chrome revalidates the top-level document on EVERY reload (`Cache-Control: max-age=0` + `If-None-Match`), and buns3 answered 200 + full body — an 11.5 kB page cost 11.5 kB per refresh. Now 0.2 kB (measured in the browser; three states confirmed: navigation within 60s = `(disk cache)` no request, reload = 304, changed ETag = 200). `etagMatches` is a pure function in `lib/` because the comparison is fiddly: RFC 9110 **weak** comparison for GET/HEAD (`W/"x"` matches `"x"`), comma-separated lists, and `*`. Stripping `W/` from the whole header instead of per entry made every list member after the first unmatchable — pinned by a test, and both that and the regex anchoring are mutation-checked.
   - `applyObjectHeaders` split into `applyValidatorHeaders` (ETag, Last-Modified, Cache-Control, Vary — sent on BOTH 200 and 304) and `applyPayloadHeaders` (Content-Type, Content-Disposition, and HEAD's Content-Length — only with a body), because **a 304 must not carry representation headers**. Auth stays first: a 304 is a statement about content the caller is entitled to.
   - **`createServer` is now split from `initServer`** (`6594afc`) — the former builds the app, the latter listens. `createServer().handle(new Request(...))` exercises routes, macros, auth and error rendering **in-process with no port** (verified). This is the seam that route-level tier-2 tests and the deferred SDK↔server integration tests both needed. `initServer` was `async` but never awaited anything — Elysia's `.listen()` is synchronous.
   - **Route-level tests** (`__tests__/caching.routes.test.ts`, 18 tests) close this out: directives per auth kind, HEAD agreeing with GET, listings/errors stating nothing (fail-closed), 304 on match, 200 on a stale validator, an overwrite invalidating an old one, `no-store` preserved on an authenticated 304, a 304 NOT bypassing auth, and the favicon route. Mutation-checked (cache leak → 3 failures, no 304 branch → 4, payload-before-conditional → 1). Cloudflare is back to "Eligible for cache" with Edge TTL following the origin header, so public objects cache at the edge and everything else bypasses.
4. **Multipart uploads** — the stated END GOAL (2026-09-04). Immediate motivation: Cloudflare caps REQUEST bodies at 100 MB (Free/Pro; 200 MB Business), so the 5 GB `maxRequestBodySize` is unreachable while proxied — a >100 MB PUT dies at the edge with an HTML 413, which the SDK surfaces as `status: 413, code: "UNKNOWN"` (exactly the proxy-answered case `fromProblem` degrades on). Downloads are unaffected: an 18.4 MB object served fine, and response size is not capped. Beyond the proxy, multipart buys resumability and parallel uploads. **Design questions to settle BEFORE code**: (a) S3's actual API (initiate / upload-part / complete-with-manifest / abort, per-part ETags) or a simpler append-to-session scheme? The S3 shape only earns its keep if existing S3 clients should work against buns3, which has never been a goal — "S3 is a reference point, not a requirement". (b) New durable state: upload sessions and parts held somewhere until completion, where today every object is one atomic move. (c) The write-order invariant changes shape: parts-durable → assemble → pointer, and assembly must itself be crash-safe. (d) A THIRD garbage class (abandoned uploads) joins stale `.tmp` and orphaned blobs — which makes the cron sweeps a prerequisite, not a parallel task. (e) Presign needs `uploadId`/`partNumber` in the canonical string, or a signature would not cover what is actually being uploaded.
5. ~~**Cron cleanup sweeps**~~ **DONE 2026-09-06** — `storage/cleanup.ts`: `sweepTempFiles` and `sweepOrphanBlobs`, results-as-values, one shared `SweepData` (`scanned/removed/skipped/errors`). Design: an `OUTCOMES` list is the source of a closed union (`removed | fresh | owned | not-a-uuid | not-a-file | vanished | error`); `emptyCounts`/`add`/`total`/`summarize` are total over it by construction, so a new outcome cannot go uncounted (the counting drifted three times while it was hand-tallied). `sweepFile` classifies one path and is shared by both sweeps (`ENOENT` on stat OR unlink = `vanished`, the upload-completed-between-my-two-calls race, never an error). `classifyRootEntry` is the policy for what the sweep will enter: non-directories skipped silently (`db.sqlite` and its `-wal`/`-shm` live in `data/`), `.tmp` skipped, a directory failing the bucket regex is logged and left alone. `sweepBucketDir` does two flat queries (existence + `select("id")`), folds entries through owned / not-a-uuid / `sweepFile`, and returns its own counts plus a `failures` count for the two non-entry failures (unreadable dir, `rmdir` refusing).
   - **The age gate is load-bearing everywhere** (`CLEANUP_OLDER_THAN_MS` = 1h, plain constant — no env seam on purpose: `Number("")` is 0 and a zero gate is a data-eater). Cutoff computed once per sweep, strict `<` on mtime. **Rowless bucket dirs are collected** (bucket delete only removes an EMPTY dir, so a bucket deleted with orphans left its dir forever) — but only when the DIRECTORY's own mtime is past the gate too: `bucketStorage.create` runs `mkdir` INSIDE its transaction, so a brand-new bucket is briefly a rowless empty dir on disk. The dir is `stat`ed BEFORE the loop, because unlinking entries bumps the dir's mtime and a stat afterwards always reads fresh. `ENOTEMPTY` on `rmdir` is swallowed as a race backstop; the `remaining` predicate (`total - removed - vanished`) is the primary path.
   - Scheduler (`modules/scheduler`): bare `setInterval` (`CLEANUP_INTERVAL_MS` = 15m), `.unref()`, in-flight guard, `Promise.allSettled` with rejections logged, `runOnStartup` so a deploy shows its report immediately, logs only when `removed || errors` and says `dry run` vs `completed`. Rejected: `@elysiajs/cron` — NO 2.0 release (`next` is a stale 1.0.0-beta.1) and its peer range `>= 1.4.0` would install against 2.0 silently, the openapi trap again; `croner` is zero-dep but buys only cron syntax, which "every 15 minutes" does not need. Production runs the sweeps for real (`CLEANUP_DRY_RUN` unset); the v0.1.1 deploy (2026-09-06) logged NO cleanup lines — which is ambiguous, because `report` is silent on zero removals, so a sweep that never ran looks identical. The startup run now logs its summary unconditionally (zeros included); interval ticks stay quiet unless `removed || errors`. `CLEANUP_DRY_RUN=1` in the Dokploy env turns it back into a report without a release.
   - `POST /_admin/cleanup` (`routes/cleanup.routes.ts`, `auth: "admin"`): body `{dryRun}` via arktype `"boolean = false"` and `validate(Cleanup, body ?? {})` (probed: a bodyless POST arrives as `undefined`; an EMPTY body with a JSON content-type is a 400 parse error before the handler). Returns `{dryRun, temp, orphans}` with per-sweep results, never unwrapped — one sweep failing must not hide the other's work (batch-delete precedent). The age is never accepted from the client; an extra key is ignored and tested. The sweeps must be `await`ed — a Promise in the response object serialises as `{}` and tsc cannot see it.
   - Tests: 13 tier-2 in `storage/__tests__/cleanup.test.ts` (data-eater first: a pointer-backed blob aged 2h survives; gate pair; non-UUID; root stray file standing in for `db.sqlite`, which the FIXTURE does not place inside `DATA_PATH`; unrecognised dir; rowless dir emptied + removed, rowless dir with fresh file stays, fresh empty rowless dir left alone; dry run reports and touches nothing) — 9 mutations all caught, and two stated gaps: dropping the `remaining` guard is invisible because the `ENOTEMPTY` backstop yields the same outcome, and `failures` feeding `errors` has no test because provoking an unreadable dir is unreliable on Windows. 5 route tests through `createServer().handle()`, 2 mutations caught. Tests wipe `.tmp` in their own `beforeEach` — `resetStorage` keeps it by design.

6. ~~**Versioning the server itself**~~ **DONE 2026-09-04** — `GET /_server` (`auth: true`) answers `{version}`: any valid key, admin or not; anonymous and unknown credentials get 401, so a deployment does not advertise its build. Route in `routes/server.routes.ts` (`routes:server`, prefix + `.get("")`, mirroring selfRoutes). Single source of truth is root `package.json` `version` → `src/lib/version.ts` (`VERSION`), read by BOTH the route and the startup log. **tsc enforces the field exists** (probed: the JSON import types structurally, so deleting `version` is a compile error — do NOT write a `?? "unknown"` fallback, it cannot fire).
   - **Lockstep DECIDED**: the server and `@buns3/sdk` carry ONE number, enforced by `test/version.test.ts` (mutation-checked: SDK drift fails 1, a leading `v` in a manifest fails 2 — the `v` lives only on the git tag). Rejected: independent versions with a documented compatibility range — that is an invariant written in prose that nothing checks, and this project does not keep those. Rejected: **an SDK runtime mismatch warning** — the two are briefly different during every deploy, so it would fire in normal operation and be tuned out; a client cannot fix a version skew anyway; and library `console.warn` is noise a consumer cannot suppress. If the SDK ever genuinely needs this it is **negotiation, not warning**, and the right shape is a capabilities document (which is why `/_server` is named for the subject, not for `version` — multipart will want exactly that).
   - SDK side: `planes/server.ts` + `types/server.ts` (`ServerResponse`, following the nine-type `<Thing>Response` convention — an `http.ServerResponse` collision is a type-only alias, not worth breaking the pattern). `createServer` IS exported from the SDK index and **collides with the server's own `createServer`** — the integration test imports it as `createServerPlane`. Naming went `/_version` → `/_meta` → `/_server`; renaming was free only because nothing consumed it yet, which is the argument for settling names before publish. 5 integration tests, all mutation-checked (hardcoded version, parsed-field shape, admin-only regression, guard removed).
   - **API docs are now opt-in**: `enabled: process.env.OPENAPI === "1"`. Found while doing this: **`NODE_ENV` is set NOWHERE in the repo and Bun does not default it** (probed: `undefined`), so the obvious `NODE_ENV !== "production"` guard is a no-op that READS like protection — the flag is positive so that unset means off. Probed: `enabled: false` genuinely unregisters the routes (the paths then 422 as invalid bucket names — same no-oracle answer as any other `_` stray), and `OPENAPI=true` is OFF (strict `=== "1"`). The exposure was live in production: `/_openapi` and `/_openapi/json` both 200 unauthenticated, and the page loads `@scalar/api-reference@latest` from jsdelivr — unpinned third-party JS executing on the same origin as the objects.
   - **Rollback reality, and the earlier note here was WRONG**: Dokploy builds from the git ref and compose uses `build:` with no `image:` and no registry, so tagging images buys nothing — rollback is a git-ref story, not a registry one. More importantly, **migrations run forward before the server starts, so a rollback runs OLD code against a NEWER schema**: additive changes survive, a dropped or renamed column does not (and Prisma renders renames as drop+add, having no rename detection). **A destructive migration is a one-way door** — stage it across two releases: add and backfill first, drop only once the previous version is no longer a rollback target.
   - Bump policy: stay 0.x; **minor = wire-contract break, patch = everything else**; 1.0 when the contract is declared stable. Precedent for wire breaks at 0.x: `KEY_*`→`API_KEY_*`, `/_admin/whoami`→`/_self`, `/openapi`→`/_openapi`, single-segment GET.
   - **STILL OPEN**: the git sha as a build identifier (needs `ARG BUILD_SHA=unknown` — `ENV BUILD_SHA=${BUILD_SHA}` with no ARG is the empty-string trap again, and empty is not nullish), and changelog/releases.

7. Later candy: **throttle `lastUsedAt`** — every authenticated request currently pays a find-and-touch `UPDATE` (measured ~60ms per `/_server` call in production, 2026-09-06, most of it the auth path); touch at most once per key per minute (compare the row's `lastUsedAt` before writing) so reads stop being writes. Not urgent at this scale. admin tester "empty bucket" button (list → batch delete, one fetch now that both exist); global data keys (bucketName null + capabilities — enforcement already supports it via `inScope`, only the CreateApiKey union forbids; parked by choice). (crypto.subtle browser signer: absorbed into the SDK item — its offline presign IS that feature.)
8. **Object versioning** — parked for thinking (raised 2026-09-04), no commitment. **The existing layout is already most of the way there**: blobs are `data/<bucket>/<uuid>` with the pointer in SQLite, so every write already mints a fresh blob with its own id — the id IS a natural version id, and it is already the ETag. Versioning is essentially "stop deleting the old pointer on overwrite". Batch delete already resolves keys to blob ids first and deletes by id ("delete the versions I saw"), so the vocabulary exists in the code. Questions to settle before anything: is it per-bucket opt-in (S3 has a bucket flag) or global? Does GET take `?versionId=` and default to latest? Does DELETE insert a delete-marker (S3) or actually remove — and how does that interact with the decided "deletes are honest, not idempotent" 404? Does listing show only the latest by default, and is there a separate way to list versions? What happens to `createdAt` semantics, currently documented as "the current version's write time"? Storage grows without bound unless there is expiry, which ties straight into the cron sweeps and would make them a lifecycle engine rather than a garbage collector. And presign would need `versionId` in the canonical string, or a signature would not cover which version is served. **The S3-clone cliff applies here more than anywhere** — version listing, delete markers and lifecycle rules are where S3 gets genuinely complicated, and "simplicity wins" may argue for a much smaller thing (keep N previous versions, restore-by-id, no markers) or for not doing it at all.

9. ~~**Logging**~~ **DONE 2026-09-06** — pino 10.3.1 (pinned), `src/lib/logger.ts` is three lines: `pino({ level: config.LOG_LEVEL, base: null, timestamp: isoTime })` — `base: null` drops pid/hostname (noise in a container, and hostname is personal-setup territory in shared logs). Modules take `logger.child({ module })`. **Exactly one bare `console.*` remains, in `config.ts`**, because the logger's level comes from config so it cannot exist yet — importing the logger there was a live import cycle (both files use the other at top level → TDZ `ReferenceError`, and WHICH one throws depends on import order). Enforced by `src/__tests__/logging-guard.test.ts`, which also fails the build on an Error logged under any key but `err` — pino serialises the stack ONLY under `err`; `{ error: e }` prints `{}` (probed). Levels: `error` = a human should look, `warn` = a best-effort step failed and something else will pick it up (orphaned blob/dir), `info` = summaries and startup. Data goes in fields, the message is a constant string.
   - **Request log** = `useLogger(logger)` in `middleware.ts`, registered first in `createServer({ logger })` (the seam a test uses to capture lines in memory). **Elysia 2.0 gotchas found here**: (1) `onRequest`/`onAfterResponse` do not exist — hooks are `transform`, `derive`, `beforeHandle`, `afterHandle`, `mapResponse`, `afterResponse`; (2) **a plugin's lifecycle hooks are LOCAL to the plugin unless `.as("global")`** — the hook fires for none of the parent's routes otherwise, and the per-hook `{ as: "global" }` option form is a silent no-op (probed all four); (3) `requestId`/`t0` are minted in a global `transform`, NOT `derive`, because the bucket macro's own transform throws its 422 before any derive runs — with derive, every validation error had no id (probed). `afterResponse` fires for 200/401/500 and unmatched 404s; `set.status` is undefined on the 200 path (`?? 200`); `authState` is present only where the auth macro ran and is widened by a local cast.
   - **Security rule, tested**: the path is logged WITHOUT its query string on every route (a presigned URL is a credential); the `Authorization` header never appears; a presigned request's line contains no `sig`/`keyId`/`expires`/`?`. 5 tests in `request-log.test.ts` via a captured pino stream. Dev: `"dev": "bun --watch --no-clear-screen ./src | bun x pino-pretty"` — **`--no-clear-screen` is load-bearing**: Bun's watch clears the terminal by writing an escape sequence to stdout, which travels down the pipe, glues onto the first log line of the reload, and makes pino-pretty print it raw (only with a TTY, so a file-redirected probe cannot reproduce it). `pino-pretty` is a devDependency; no transport runs in the container. The test preload pins `OPENAPI=0` because Bun auto-loads `.env` into tests and a developer's `OPENAPI=1` leaked in. **`LOG_CAPTURE=1`** (config flag, set by the preload) points the ROOT logger at an in-memory sink exported as `capturedLogs` from `lib/logger.ts`, so every module child is capturable in tests and test output carries no JSON lines; the `createServer({ logger })` seam still exists for the request hook. Also logged: **effective config at startup** (info, no secrets in it), **auth rejections at debug with the real reason** (`module: auth`/`api-keys` — the no-oracle policy hides the reason from the client, so the log is the only place it exists; fields never include token, hash or sig; keyId is a public fingerprint), **control-plane lifecycle at info** (bucket created/updated/deleted, key created/deleted — never the token), `fileStorage.init` at debug. Deliberately NOT logged: per-object operations or auth successes — the request line already has them. The guard also forbids `reason` as a log key (a rejected promise's `.reason` is an Error); a string reason is logged as `failure`. Shutdown logs `restarting` only when `process.execArgv` has `--watch` AND the signal is SIGTERM (a `--watch` reload IS a SIGTERM; Ctrl+C is SIGINT). **`GET /_health`** (unauthenticated, `routes:server`): readiness = `db.orm.Bucket.select("name").first()` (an ORM ping — the raw lane works too, `db.raw.sql`…`.returnsRow({ col: "sqlite/integer@1" }).build()` executed with `db.runtime().query(plan)`, NOT `execute`, and only registered codec ids, but it is not worth its footguns here) plus `fs.access(.tmp, W_OK)` (a `readdir` passes on a read-only volume, which is the case the check exists for). `{status, checks: {db, storage}}`, 200 or **503** so the orchestrator restarts rather than reading sad JSON. The compose healthcheck polls it every 10s: a healthy answer is logged at `debug`, a 503 at `info`; `GET /` is a human page and stays at `info`. Health failures log `error`, NOT `fatal` — `fatal` means the process is exiting (pino flushes synchronously for it) and nothing in `src/` exits except config at boot, which predates the logger. `CLEANUP_OLDER_THAN_MS`/`CLEANUP_INTERVAL_MS` were documented env knobs that did nothing until 2026-09-07: three consumers still read the old hardcoded constants — now removed, all read `config`.

PARKED (deliberately): upstream bug reports — opted out (2026-08-30). Receipts stay in git/transcripts if ever revisited: Elysia Content-Range on 200s, schema-slot Standard Schema messages dropped, macro autocomplete, derive-union ctx collapse; @elysia/openapi typebox imports (patched), RegExp exclude, peer range; Prisma `now()` codec round-trip; Babel 8 standalone react preset vs classic script tags.

## Working with the maintainer

Prefers advice over written code (asks explicitly when code is wanted). No AI attribution in commits, ever. Values: decide-deliberately, README-the-decision, one-lesson-per-bug. **This repo is public: personal setup details (machines, hosts, domains, shell config, habits) never go into tracked files.**
